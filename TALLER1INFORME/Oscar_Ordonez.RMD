---
title: "Curso de Tratamiento de datos, modelización estadística y visualización dinámica con R - Taller 1"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
#summary(cars)

5+10
15.3*23.4
sqrt(16)
producto = 15.3 * 23.4
#Asignacion de variables
producto1 <- 15.3 * 23.4

#función logaritmo
log (producto)
#entropia
log (producto , base=2)
"hola poke"

# mal escrito, no esta asignando 
x<-1:5

#bien escrito
x <- 1:5

#genera secuencia de numeros reales
seq(1, 5, 1)


#scan sirve para ingresar valores (doble enter para terminar)
z <- scan()

#repeticion
rep(1,20) #agregamos el valor 1 , 20 veces

#repetir del número 1 - 3 , 30 veces
rep(1:3, 30)

rep(seq(1,3),30)

#utilizando each
rep(1:4, each=2)

#argumento times
rep(1:4, c(2,2,2,2))


sum(x)
prod(x)
#encontrar un valor especifico
which.max(c(3,14,15,2))

#busca la posición de un número
which(c(3,14,15,2)==3)

which.min(c(3,14,15,2))

#decrementa
sort(x, decreasing= TRUE)

#funcion

f_x <- function(x)
{
	y <- x^2
	cat("El cuadrado de x es: " ,y,"\n")
	return (y)
}

f_x(5)

#getwd() devuelve el valor donde se esta ejecutando el r



```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
#plot(pressure)
library(ggplot2)
data("diamonds")

plot(diamonds$carat, diamonds$price, col=diamonds$color, pch=as.numeric(diamonds$cut))
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
